name: Selenium Crawl and Post

on:
  workflow_dispatch:  # 수동 실행만

jobs:
  selenium-job:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install dependencies
      run: npm install
    
    - name: Install Chrome and ChromeDriver
      run: |
        # Chrome 설치
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable
        
        # Chrome 버전 확인
        google-chrome --version
        
        # ChromeDriver 설치 (npm에서 자동 처리)
        npm list chromedriver || echo "ChromeDriver will be installed with npm"
    
    - name: Test Selenium Setup
      env:
        HEADLESS: 'true'
        GITHUB_ACTIONS: 'true'
      run: |
        echo "🧪 Selenium 설정 테스트"
        node test-selenium.js
    
    - name: Run Selenium Crawler
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        NAVER_ID: ${{ secrets.NAVER_ID }}
        NAVER_PASSWORD: ${{ secrets.NAVER_PASSWORD }}
        HEADLESS: 'true'
        GITHUB_ACTIONS: 'true'
      run: |
        echo "🚀 Selenium 크롤러 시작"
        echo "🥷 Undetected 모드로 실행..."
        node selenium-undetected.js || {
          echo "⚠️ Undetected 실패, 기본 크롤러 시도"
          node selenium-all-in-one.js all
        }
        echo "✅ 완료"
    
    - name: Upload screenshots if failed
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: debug-screenshots
        path: |
          *.png
          *.jpg